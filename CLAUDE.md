# CLAUDE.md — 단어 찾기 도우미

> 이 파일은 프로젝트 규칙서 초안입니다. 이 프로젝트를 실제로 개발하기로 하면
> 이 파일을 `CLAUDE.md`로 이름을 바꾸세요(한 폴더에 CLAUDE.md는 하나만 자동 인식됨).
> 원본 기획: [PRD2.md](PRD2.md), [prd_lite2.md](prd_lite2.md)

## 공통 작업 규칙 (반드시 지킬 것)

- **모든 설명과 주석은 한국어로 작성한다.**
- **새 파일은 `my-app` 폴더 안에만 만든다.** (폴더 밖에 파일을 생성하지 않는다.)
- **코드를 바꾸면 반드시 무엇을 왜 바꿨는지 한 줄로 알려준다.**
- **`.env` 등 비밀 정보 파일은 `.gitignore`에 등록해 두고, 절대 커밋하지 않는다.**
- **파일을 지워야 할 때는 바로 삭제하지 않는다.** `trash-can` 폴더를 만들어 그 안으로 옮겨만 두고, 작업이 끝난 뒤 사용자가 직접 확인하고 삭제한다.
- **이미 설치된 서브에이전트(bkit의 `gap-detector` 등)는 필요할 때마다 적극 활용한다.**
- **HTTP 응답 헤더 값에는 한글 등 비 ASCII를 넣지 않는다**(예: 인증 `realm`) — latin-1만 허용돼 응답이 깨진다. 한글은 헤더가 아닌 본문에.
- **검증은 실제 서버로 한다**: 디버그를 끈 상태에선 코드·템플릿(HTML)을 바꾼 뒤 서버를 재시작해야 반영되고, 한글이 든 요청 본문은 파일·UTF-8로 보낸다(인라인 `curl`은 한글이 깨질 수 있어 오판 위험).
- **AI 응답은 형식을 신뢰하지 말고 방어적으로 파싱한다** — 첫 완전한 JSON 객체만 추출(뒤에 잡담이 붙어도 500 오류 금지).
- **네트워크·AI 호출에는 반드시 제한 시간을 건다**(화면 fetch 25초·서버 AI 45초) — 응답이 멈춰도 버튼 등 UI 잠금이 영영 안 풀리는 일이 없게, 잠금 해제는 `finally`에서 보장한다.
- **암묵적 기본값에 의존하지 않는다** — 제한 시간 등 중요한 동작 값은 설정을 안 해도 "없음"이 아니라 어딘가의 기본값(예: AI 라이브러리 10분)이 몰래 적용된다. 의도한 값을 코드에 명시하고 이유를 주석으로 남긴다.
- **Windows용 `.bat`은 CP949 인코딩 + CRLF 줄바꿈으로 저장한다** — UTF-8이면 cmd가 한글을 깨뜨리고, LF 줄바꿈이면 줄을 잘못 읽어 실행이 실패한다.
- **AI가 모르는 것을 확신하게 만드는 프롬프트를 쓰지 않는다** — "무조건 후보를 채워라" 같은 지시는 지어내기를 부른다. 확인 안 되면 **결과를 비우고 이유를 밝히도록** 지시하고, 실존 인물·시사는 웹검색으로 확인된 것만 답하게 한다(범죄·명예 등 민감 사안은 특히 엄격히).

## 작업 절차 (검증 루프)

코드를 만들거나 바꿀 때는 **매번 아래 루프를 반복한다.**

1. **변경한다** — 코드를 수정/작성한다.
2. **결과를 직접 확인한다** — 브라우저로 열거나 실행해서 실제로 동작하는지 눈으로 본다.
3. **스스로 코드 리뷰한다** — 방금 바꾼 코드를 직접 검토한다(버그·규칙 위반·누락).
4. **문제가 있으면 고치고 다시 1)로 돌아간다.**
5. **통과하면** 무엇을 왜 바꿨는지 **한 줄로 요약**한다.

> 확인·리뷰를 건너뛰고 "됐다"고 보고하지 않는다. 실패하면 실패했다고 그대로 알린다.

## 프로젝트 개요

떠오르지 않는 단어를 **음성으로 설명**하면, 일반 단어·전문 용어·인명까지
가장 적확한 **단어 후보 3~5개**를 빠르게 찾아주는 도구. "설단 현상"으로
대화가 막히지 않게 돕는다. 중장년·노년층 등 **본인 1인용**.

- **성공 기준:** 후보 5초 이내 / 한 번에 3~5개 / 찾던 단어 포함 체감 70%+
- **핵심 원칙:** 확실하지 않은 인명·사실은 **지어내지 않고 '확실하지 않음' 표시.** 타이핑 없이 음성만으로 완료 가능.

## 기술 스택

| 영역 | 선택 |
|------|------|
| 화면(프론트) | HTML / CSS / JavaScript (바닐라) |
| 음성 입력 | 브라우저 내장 **Web Speech API** (마이크→텍스트) |
| AI 모델 | **OpenAI GPT-4o** (`gpt-4o`) — 두루뭉술한 설명으로 적확한 단어 추론 (사용자 선택: OpenAI) |
| 백엔드 | **키 보호용 최소 중계 서버**(설계 옵션 B) — 화면·음성은 브라우저, AI 호출만 서버가 중계 |
| 대상 플랫폼 | **모바일 우선** (휴대폰에서 바로 꺼내 쓰는 상황) |

> React/Next.js·앱 배포 등은 1인용 현 단계에서 과함. **단순함을 유지한다.**

## AI가 지켜야 할 핵심 규칙 (도메인)

1. **음성→텍스트 변환 결과를 화면에 보여주고 사용자가 수정 가능하게** 한다(오인식 대비). 수정된 설명으로 다시 찾는다.
2. **후보는 한 번에 3~5개**, 설명한 뜻과 가장 잘 맞는 순서대로.
3. **각 후보에 한 줄 뜻풀이** 를 붙여 바로 확인 가능하게.
4. **대상:** 일반 단어·전문 용어·인명. 전문 용어는 일반 대화 수준까지. 못 찾으면 비슷한 뜻의 단어 제시.
5. **한국어·영어 단어를 함께 제시** (한국어 대화 중 영어 혼용 대비). 다른 외국어는 대상 아님.
6. **확실하지 않은 인명·사실은 지어내지 말고 '확실하지 않음' 표시.**
7. **상세 보기:** 고른 단어의 뜻 + 실제 예문 1~2개 + 유의어 2~3개.

## 범위 / 비범위

- **범위:** 음성 설명 → 단어 후보 찾기(기능1) · 후보의 뜻·예문 상세 보기(기능2)
- **비범위(만들지 말 것):** 검색 이력·단어장(즐겨찾기) / 로그인·다기기 동기화 / 영어 외 외국어 / 발음 듣기(TTS) / 오프라인 사용

## 화면 구성

1. **메인(음성 입력)** — 큼직한 [마이크] 버튼, 인식된 설명 텍스트창(수정 가능), [다시 찾기], (대체) 타이핑 입력창. 상태표시("듣는 중…", "단어 찾는 중…").
2. **후보 목록** — 카드 3~5개(단어 + 한 줄 뜻 + 언어 표시 한/영 + '확실하지 않음' 해당 시). 카드 클릭 시 상세로.
3. **상세** — 뜻, 예문 1~2개, 유의어 2~3개, [뒤로].

## 데이터 (저장 없음 — 화면 임시 데이터만)

- **인식된 설명:** 음성→텍스트 결과(수정 가능)
- **후보:** 단어, 언어(한/영), 종류(일반/전문용어/인명), 한 줄 뜻, 확실성
- **상세:** 뜻, 예문(1~2), 유의어(2~3)
- 음성·설명은 앱에 **저장하지 않음.**

## 보안 · 개인정보

- **API 키는 코드에 직접 넣지 않는다.** 환경변수(`.env`)로 관리, 저장소·외부에 올리지 않음. `.env`는 `.gitignore`에 포함.
  - ⚠️ 순수 브라우저에서 AI를 직접 부르면 키가 노출되므로, **AI 호출은 반드시 키를 서버에만 보관하는 최소 중계 서버(옵션 B)** 를 경유한다. (2026-07-23 결정)
- 음성 입력은 단어 찾기 목적으로만 사용, 별도 저장 안 함. **브라우저 음성인식은 외부 서비스(예: 구글)로 전송될 수 있음** → 민감한 개인정보는 말하지 않도록 안내.
- 인명은 **공개적으로 알려진 인물** 위주, 개인 신상정보 수집·축적 금지.

## 예외 처리 방침

- **마이크 권한 거부·마이크 없음:** 원인 안내 + **타이핑 입력 대체 수단** 제공.
- **시끄러운 환경·인식 실패:** "다시 한 번 말씀해주세요" + 변환 텍스트 직접 수정 가능.
- **설명이 짧거나 모호:** 억지로 후보 내지 말고 "조금 더 자세히 설명해주세요".
- **단어가 아니라 문장·개념:** '단어 찾기'가 목적임을 알리되 가장 가까운 단어·표현 제시.
- **음성인식 미지원 브라우저·인터넷 끊김:** 타이핑 안내 / "인터넷 연결이 필요합니다".
- **결과 지연:** "단어 찾는 중…" 표시.

## 코딩 컨벤션

- 주석·UI 문구·커밋 메시지는 **한국어**.
- **모바일 우선** — 큰 글씨, 큼직한 버튼, 요소 적고 직관적. 마이크 버튼이 한눈에 띄게.
- 의존성은 최소로. 기능은 PRD의 **must 2가지**에만 집중, 요청하지 않은 기능 추가 금지.

## 완료 체크리스트

□ 음성→텍스트 변환·수정 □ 후보 3~5개 5초 이내 □ 한 줄 뜻 표시
□ 상세(뜻·예문·유의어) □ 다시 찾기 □ 마이크/인식 예외 안내
