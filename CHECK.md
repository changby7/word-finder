# CHECK.md — 단어 찾기 도우미 (점검 결과)

> 갱신: 2026-07-24 · 대상: 이 폴더의 앱 하나(단독 점검) · 기준: 혼자/로컬 사용 v1

## 판정: ✅ 통과 (혼자·로컬 사용 기준)
- 성공 기준 실측 통과, 지적된 Gap 전부 반영, 보안 코드 조치 완료.
- 단, **인터넷 공개용은 아님** — 공개하려면 아래 "남은 항목"의 배포/키 항목을 먼저 해결해야 함.

---

## 1. 성공 기준 판정 (PLAN.md 기준, 실측)
| 성공 기준 | 판정 | 근거(실측) |
|---|---|---|
| 후보 표시 속도 **(기준 갱신 2026-07-24: 일반 단어 5초 / 웹검색 필요 시 15초 이내)** | ✅ 통과 | 일반 단어 3.7초 · 시사·인물 질문(웹검색) 11.2초 |
| 3~5개 후보 + 한 줄 뜻 + 언어(한/영) | ✅ 통과 | 후보 5개, 한·영 혼합, 각 뜻 존재 |
| 상세 = 뜻 + 예문 1~2 + 유의어 2~3 | ✅ 통과 | 뜻+예문 2+유의어 2 확인 |
| 확실하지 않은 인명·사실 "확실하지 않음" 표시 / 지어내지 않음 | ✅ 통과 | 확인 불가한 시사·인물 질문에 후보 0개 + 사유 안내(이전엔 실존 인물 이름을 지어냈음) |
| 타이핑 없이 음성만으로 완료(마이크 불가 시 타이핑) | ✅ 통과 | Web Speech API + 타이핑 대체 + 예외 안내 |
| 찾던 단어 포함 체감 70%+ | ⏸ 본인 확인 | 소표본 양호(연기·아인슈타인 적확), 반복 사용으로 체감 판단하는 주관 항목 |

## 2. Gap(설계 대비 차이) 반영 상태
초기 gap-detector가 지적한 8건 → **전부 반영 완료**.
- 해결: 5초 계측 표시, 문장·개념 목적 안내(note), 짧은 설명 되묻기(4자 가드), 후보 3~5개 보장(필수필드 필터·최대 5개), 예문 1~2·유의어 2~3 제한, 버튼 라벨 정정, 스테일 문구 제거, 마이크 없음(audio-capture) 안내.

## 3. 보안 점검 결과 (security-architect)
**적용 완료(코드):**
- debug 기본 꺼짐(FLASK_DEBUG=1일 때만) — 디버거 원격코드실행 방지
- 레이트리밋(IP당 60초 20회) + 입력 길이(설명 500·단어 100자) + MAX_CONTENT_LENGTH 64KB — 요금폭탄/DoS 방지
- 보안 헤더(X-Frame-Options·nosniff·Referrer-Policy·CSP)
- 예외 원문 미노출(일반 문구 + 서버 로그)
- 프롬프트 인젝션 방어(설명을 데이터로 격리)

**양호(원래 잘 됨):** 옵션 B 키 은닉(프론트에 키 없음, /health는 키 값 미노출), 프론트 textContent 사용(XSS 방어), 음성 외부 전송 주의 안내 존재, 설명·단어를 서버에 저장·로그하지 않음.

---

## 4. 이후 변경 이력 (2026-07-23 ~ 07-24)
| 변경 | 내용 · 이유 |
|---|---|
| 접근 인증 | `.env`의 `APP_PASSWORD`로 켜는 HTTP Basic 로그인(비우면 꺼짐). 헤더 realm에 한글을 넣어 로그인이 안 뜨던 버그 수정(ASCII로) |
| JSON 파싱 방어 | AI가 응답 뒤에 잡담을 붙이면 500 오류 나던 것을 첫 완전한 JSON만 추출하도록 수정 |
| **버튼 잠김 버그 수정** | 연속 사용 시 [단어 찾기] 버튼이 안 풀리던 문제 — 화면 fetch **25초**·서버 AI **45초** 제한 시간 추가, 초과 시 안내 후 버튼 복구. 연속 5회 실행 검증(5/5 정상) |
| UX 개선 | 설명창에서 **Enter 키로 바로 찾기**(Shift+Enter는 줄바꿈), 동시 요청 처리(threaded) |
| **웹검색 추가 + 지어내기 차단** | 실존 인물 이름을 지어내던 문제(구속·명예 맥락) 대응 — 후보 찾기에 웹검색(`ask_with_web_search`) 적용, 프롬프트에 "확인 안 되면 후보를 비우고 이유를 밝힌다" 규칙 추가. 결과: 확인 불가 질문에 후보 0개+사유 안내, 일반 단어 찾기는 3.7초로 정상 |
| 속도 기준 완화 | 웹검색 도입으로 5초 기준을 **일반 단어 5초 / 웹검색 15초**로 조정, 화면 문구도 "N초 만에 찾음 (웹검색 포함)"으로 변경. 화면 대기 25초→60초 |
| 실행 편의 | 더블클릭으로 서버를 켜는 `실행.bat` 추가(CP949·CRLF) |

## 남은 항목 (우선순위 순)
1. 🔴 **OpenAI 옛 키 삭제(revoke)** — 유출된 옛 키가 아직 살아있음. (사용자 선택으로 현재 보류) → 최소한 키에 **월 사용 한도** 설정 권장.
2. ✅ **접근 인증(로그인/비밀번호)** — 완료(2026-07-23). `.env`에 `APP_PASSWORD`를 넣으면 브라우저 로그인(HTTP Basic) 요구, 비우면 인증 꺼짐(로컬 사용용). HTTP Basic이라 실제 공개 시엔 3번(HTTPS)과 함께 써야 안전.
3. 🟠 **HTTPS + 프로덕션 서버(waitress/gunicorn + nginx)** — 공개 배포 시 필요. 특히 마이크(Web Speech API)는 공개 주소에서 HTTPS가 사실상 필수. (로컬 사용엔 불필요)

> 1~3번은 "인터넷 공개"를 실제로 할 때 필요한 항목입니다. 혼자·로컬로 쓰는 현재 상태에서는 위 판정대로 통과입니다.
