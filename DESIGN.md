# DESIGN.md — 단어 찾기 도우미

> 바탕: [PLAN.md](PLAN.md) · [PRD2.md](PRD2.md) · 규칙서: [CLAUDE.md](CLAUDE.md)

## 1. 화면 구성 (무엇이 어디에 보이나)

### 화면 1 — 메인(음성 입력)
```
┌─────────────────────┐
│    단어 찾기 도우미   │
│                     │
│   ┌───────────────┐ │  ← 인식된 설명 텍스트(수정 가능)
│   │ "회의를 뒤로…" │ │
│   └───────────────┘ │
│                     │
│        ●            │  ← 큼직한 마이크 버튼(한눈에 띄게)
│      마이크          │
│                     │
│   [ 단어 찾기 ]      │  ← 설명으로 후보 찾기
│   ┈ 또는 타이핑 ┈    │  ← 마이크 안 될 때 대체 입력
│   상태: 듣는 중…     │
└─────────────────────┘
```

### 화면 2 — 후보 목록
```
┌─────────────────────┐
│ ‹뒤로   후보 단어    │
│ ┌─────────────────┐ │
│ │ 연기하다 [한][일반]│ │  ← 카드 = 단어 + 한 줄 뜻 + 언어(한/영) 배지
│ │ 뒤로 미루다      │ │     + 종류(일반/전문용어/인명) 배지
│ │                 │ │     (+ '확실하지 않음' 배지 해당 시)
│ ├─────────────────┤ │
│ │ postpone  [영]   │ │
│ │ 미루다·연기하다   │ │
│ └─────────────────┘ │  ← 3~5개, 탭하면 상세로
└─────────────────────┘
```

### 화면 3 — 상세
```
┌─────────────────────┐
│ ‹뒤로   연기하다     │
│ 뜻: 정해진 때를 뒤로… │
│ 예문: 회의를 다음 주로│  ← 예문 1~2개
│      연기했다.       │
│ 유의어: 미루다, 늦추다 │  ← 유의어 2~3개
└─────────────────────┘
```

## 2. 데이터 흐름 (입력 → 처리 → 출력)

```
[입력]  마이크로 "그거 있잖아, ○○할 때 쓰는…" 음성 설명
   │
   ▼
[처리]  ① 브라우저 음성인식(Web Speech API)이 음성 → 텍스트 변환
        ② 변환된 설명을 화면에 표시 → 사용자가 확인·수정
        ③ 설명을 [중계 서버]로 전송 → 서버가 .env의 키로 OpenAI GPT-4o 호출
           (한/영·종류·한 줄 뜻·확실성; 못 찾으면 유사어, 불확실 인명은 '확실하지 않음')
           ※ 옵션 B: AI 키는 브라우저에 노출되지 않고 중계 서버에만 보관
        ④ 후보 하나 선택 → (같은 경로로) 그 단어의 뜻·예문·유의어를 다시 요청
   │
   ▼
[출력]  후보 목록 화면 → (선택 시) 상세 화면

  [브라우저] ──설명──▶ [중계 서버(키 은닉)] ──▶ [OpenAI GPT-4o]
      ▲                                              │
      └──────────────── 후보/상세 ───────────────────┘
```
- **저장 없음:** 음성·설명은 앱·서버에 저장하지 않음(그때그때 처리, 중계만)
- **주의:** 브라우저 음성인식은 외부 서비스로 전송될 수 있어, 민감한 개인정보는 말하지 않도록 안내
- **오류 시:** 마이크 거부→타이핑 대체, 인식 실패→"다시 말씀해주세요", 미지원 브라우저·인터넷 끊김 안내, **설명이 짧/모호→"조금 더 자세히 설명해주세요", 단어가 아니라 문장·개념→'단어 찾기' 목적 안내 후 가장 가까운 표현 제시**

## 3. 기술 선택 — 난이도별 3옵션

> ✅ **선택: 옵션 B** (2026-07-23 결정) — 키를 숨기는 작은 중계 서버를 두는 B로 구현한다.
> 비개발자용 한 줄 설명입니다. **추천은 옵션 B**(키를 지키는 작은 안전장치 추가).

| 옵션 | 난이도 | 한 줄 설명 |
|------|--------|-----------|
| **A** | 쉬움 | 브라우저(HTML/JS)만으로 음성인식 + AI를 직접 부르기 — 설치 없이 가장 간단하지만 **AI 키가 화면 코드에 그대로 노출**돼요. (내 PRD의 '백엔드 없음'과 동일) |
| **B ⭐추천** | 보통 | 화면·음성은 브라우저 그대로 두고, **키만 숨겨주는 아주 작은 중계 서버 하나**를 통해 AI 호출 — 화면은 똑같이 간단하면서 키가 안전. |
| **C** | 어려움 | React Native/Flutter로 정식 모바일 앱을 만들어 앱스토어 배포 — 제대로지만 혼자 쓰기엔 **배울 것·준비할 것이 너무 많아요.** |

**추천 이유(B):** PRD는 '백엔드 없음(A)'을 골랐지만, 브라우저에서 AI 키를 직접 쓰면 키가 노출되는 게 실제 문제입니다. 화면은 그대로 두고 **키를 가려주는 작은 서버 한 조각**만 더하면, 간단함은 유지하면서 안전 문제를 막을 수 있습니다. (키 노출을 감수하고 최대한 단순하게 가고 싶으면 A도 선택 가능)

> **PRD2 옵션과의 매핑:** PRD2의 옵션 2(웹앱+브라우저 직접 호출) ≈ 여기 **옵션 A**, PRD2 옵션 3(앱 배포) ≈ **옵션 C**. **옵션 B(중계 서버)는 PRD2에 없던 새 항목**으로, 키 노출 문제를 막기 위해 추가한 선택지다. → 이 결정은 PRD2 7·8번, PLAN에도 반영함.

## 4. 성공·테스트 기준 (측정 방법 포함)

| 성공기준 | 측정 방법 |
|---|---|
| **5초 이내** 후보 표시 | 설명 확정 시각 ~ 후보 렌더 시각 차이를 브라우저에서 계측(체감+콘솔 타임스탬프). 지연 시 "단어 찾는 중…" 표시. |
| 후보 **3~5개** + 한 줄 뜻 | 응답 후보 개수·각 뜻 존재 확인 |
| 찾던 단어 포함 **체감 70%** | 혼자 쓰는 도구이므로 본인 사용 10회 중 포함 횟수를 수기로 기록해 체감 판단 |
| **음성만으로 완료** | 타이핑 없이 마이크→후보→상세 도달 가능 확인(마이크 불가 시 타이핑 대체 동작) |
| **'확실하지 않음'** 표시 | 불확실 인명/사실에 배지 노출 확인(지어내지 않음) |
| 상세 = 뜻+예문1~2+유의어2~3 | 상세 화면 항목 존재 확인 |

**테스트 시나리오:** ①정상("회의를 뒤로 미루는 거"→'연기하다/postpone' 5초 내) ②인식오류(텍스트 수정 후 재검색) ③인명(불확실→'확실하지 않음') ④예외(마이크 거부→타이핑 대체).
**완료 체크리스트:** □음성→텍스트 변환·수정 □후보 3~5개 5초 이내 □한 줄 뜻 □상세(뜻·예문·유의어) □다시 찾기 □마이크/인식 예외 안내
