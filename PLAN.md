# PLAN.md — 단어 찾기 도우미

> 원본 기획: [PRD2.md](PRD2.md) · 규칙서: [CLAUDE.md](CLAUDE.md)

## 이번 사이클 목표
떠오르지 않는 단어를 **음성으로 설명하면 적확한 단어 후보 3~5개를 찾아주고**, 후보를 고르면 뜻·예문·유의어를 보여주는 v1 모바일 웹앱을 완성한다. (브라우저 음성인식 + OpenAI GPT-4o, **키 보호용 최소 중계 서버 포함** — 설계 옵션 B, 모바일 우선)

## 성공 기준
- 음성 설명 후 후보가 **5초 이내** 표시
- 한 번에 **3~5개 후보**, 각 후보에 **한 줄 뜻풀이** + 언어 표시(한/영)
- 찾던 단어가 후보에 포함되는 비율 **체감 70% 이상**
- **타이핑 없이 음성만으로** 검색 완료 가능(단, 마이크 불가 시 타이핑 대체 제공)
- 확실하지 않은 인명·사실은 지어내지 않고 **"확실하지 않음" 표시**
- 상세 화면에서 **뜻 + 예문 1~2개 + 유의어 2~3개** 확인 가능

## 작업 순서 (기반 작업부터)
1. 프로젝트 뼈대 구성 — 모바일 우선 HTML/CSS/JS 페이지 + `.gitignore`, 키 관리 방침(`.env`) 정리
2. 화면 골격·전환 — 메인(마이크·설명 텍스트창·타이핑 대체)·후보목록·상세 3화면과 간단한 전환 로직
3. 음성 입력 — Web Speech API로 마이크→텍스트 변환, 화면에 표시하고 **사용자가 수정 가능**하게
4. 외부 연동 래퍼 — **키 숨김용 최소 중계 서버(옵션 B)** 를 통해 OpenAI GPT-4o(`gpt-4o`) 호출(키는 서버의 `.env`에만 보관)
5. 기능1 후보 찾기 — 설명 텍스트 → 후보 3~5개 요청(한/영·종류·확실성·못 찾으면 유사어, **전문용어는 일반 대화 수준까지** 규칙 프롬프트) → 후보 카드 렌더
6. 기능2 상세 보기 — 후보 선택 → 뜻·예문 1~2개·유의어 2~3개 요청·렌더
7. 다시 찾기 흐름 — 설명을 고치거나 보태서 재검색 + "확실하지 않음" 표시 반영
8. 상태/예외 처리 — "듣는 중…"·"단어 찾는 중…" 표시 + 마이크 권한 거부(→타이핑)·인식 실패·미지원 브라우저·인터넷 끊김 안내 + **설명이 짧/모호하면 "조금 더 자세히" 유도** + **단어가 아니라 문장·개념이면 '단어 찾기' 목적 안내 후 가장 가까운 표현 제시**
9. 마무리 다듬기 — 모바일 UX(큰 글씨·큰 버튼) + 민감정보 음성 주의 안내 + 완료 체크리스트 점검

## 비범위 (이번에 안 만듦)
검색 이력·단어장 / 로그인·다기기 동기화 / 영어 외 외국어 / 발음 듣기(TTS) / 오프라인 사용
